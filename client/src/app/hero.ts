// Definimos una clase para representar nuestro modelo Hero
export class Hero {
    // Definimos los campos de nuestro modelo
    name: string;
    power: string;

    // Constructor que inicializa las variables
    constructor(name: string, power: string) {
        this.name = name;
        this.power = power;
    }
}
